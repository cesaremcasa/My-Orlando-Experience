# Local data boundary

Raw PDFs, processed source-derived text, embeddings, and indexes are local
operator inputs and are ignored by Git. Download or rebuild them only when you
have permission to use the source material; see `../DATA_PROVENANCE.md`.
